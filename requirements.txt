# Dependencies for FloorPlanTo3D API
# Phase 1: detector reverted to Mask R-CNN (Keras/TensorFlow).
# torch / transformers / training stack removed; your other upgrades kept.

# ==========================================
# Core ML  (Mask R-CNN — Keras / TensorFlow)
# ==========================================
# macOS / Apple Silicon (matches the Simsys base this .h5 was proven on):
tensorflow-macos==2.13.0
tensorflow-metal==1.0.1
# --- Linux / Windows: comment the TWO lines above and use this one instead: ---
# tensorflow==2.13.0
keras==2.13.1

# ==========================================
# Computer Vision & Image Processing
# ==========================================
opencv-python==4.8.1.78
scikit-image==0.21.0
Pillow==10.0.1
imageio==2.31.5
imgaug==0.4.0

# ==========================================
# OCR  (kept from your upgrade — PaddleOCR + fuzzy matching)
# ==========================================
paddleocr==2.7.3
paddlepaddle==2.5.2
thefuzz==0.20.0
python-Levenshtein==0.23.0

# ==========================================
# Scientific Computing
# ==========================================
# numpy pinned to 1.24.x for TensorFlow 2.13 + Matterport Mask R-CNN
# compatibility. (Your Mask2Former branch used 1.26.4 — too new for TF 2.13.)
numpy==1.24.3
scipy==1.11.3
matplotlib==3.7.2

# ==========================================
# Web Framework
# ==========================================
Flask==2.3.3
Flask-Cors==4.0.0
Werkzeug==2.3.7

# ==========================================
# Validation & API Documentation (kept from your upgrade)
# ==========================================
pydantic>=2.0.0
flask-openapi3>=3.0.0

# ==========================================
# IFC Export (kept from your upgrade)
# ==========================================
ifcopenshell==0.7.0

# ==========================================
# Utilities
# ==========================================
h5py==3.9.0
PyYAML==6.0.1
requests==2.31.0
Cython==3.0.2
psutil>=5.9.0
python-dateutil==2.8.2

# ==========================================
# Production server
# ==========================================
gunicorn>=21.2.0

# ==========================================
# REMOVED in Phase 1 (Mask2Former / training only):
#   torch, torchvision, transformers, accelerate,
#   datasets, pycocotools, torchmetrics, tqdm
# Re-add these only if you later pursue Option B (train Mask2Former).
# ==========================================
